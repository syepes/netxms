SELECT create_hypertable('idata', 'idata_timestamp', 'node_id', chunk_target_size => 'estimate', migrate_data => true) INTO temp;
SELECT create_hypertable('tdata', 'tdata_timestamp', 'node_id', chunk_target_size => 'estimate', migrate_data => true) INTO temp;
DROP TABLE temp;
